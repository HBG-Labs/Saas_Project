import { PagePlaceholder } from '@/components/feedback/PagePlaceholder';

export default function HistoryPage() {
  return (
    <PagePlaceholder
      title="Historique"
      description="La table `tool_history` est en place, en ajout seul et isolée par utilisateur."
      plannedFor="Phase 4"
    />
  );
}
