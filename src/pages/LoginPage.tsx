import { PagePlaceholder } from '@/components/feedback/PagePlaceholder';

export default function LoginPage() {
  return (
    <PagePlaceholder
      title="Connexion"
      description="La plomberie d'authentification est en place (session, rafraîchissement de jeton, protection des routes). Le formulaire sera développé en Phase 2."
      plannedFor="Phase 2"
    />
  );
}
