import { PagePlaceholder } from '@/components/feedback/PagePlaceholder';

export default function ReferencesPage() {
  return (
    <PagePlaceholder
      title="Références techniques"
      description="Ressources et abaques métier. La table dédiée n'est volontairement pas créée tant qu'il n'y a pas de contenu à stocker."
      plannedFor="Phase 5"
    />
  );
}
