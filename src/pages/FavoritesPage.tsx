import { PagePlaceholder } from '@/components/feedback/PagePlaceholder';

export default function FavoritesPage() {
  return (
    <PagePlaceholder
      title="Favoris"
      description="La table `favorites` et ses politiques RLS sont en place. L'interface sera développée quand il y aura des outils à mettre en favori."
      plannedFor="Phase 4"
    />
  );
}
