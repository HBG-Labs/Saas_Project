import { PagePlaceholder } from '@/components/feedback/PagePlaceholder';

export default function DashboardPage() {
  return (
    <PagePlaceholder
      title="Tableau de bord"
      description="Route privée : accessible uniquement avec une session valide. Le contenu réel sera développé une fois les outils disponibles."
      plannedFor="Phase 4"
    />
  );
}
